import 'package:flutter/material.dart';

class profileStatics extends StatefulWidget {
  const profileStatics({Key? key}) : super(key: key);

  // PROFILE
  static int profileIndexTabs = 0;


  // BASKET
  static int basketIndexTabs = 0;

  @override
  State<profileStatics> createState() => _profileStaticsState();
}

class _profileStaticsState extends State<profileStatics> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
